export enum AppRoutes {
    index = '/',
    register = '/register',
    activate = '/activate',
    activateRequest = '/activate/:id',
    login = '/login',
    upload = '/upload',
    error = '*'
}
